# ============================================================
# Pote - CI Pipeline
# ============================================================
# Runs on every push and PR to main.
# Tests, lints, and builds all services.

# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   # ---------- Go Services ----------
#   go-services:
#     name: Go Services - Lint & Test
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         service:
#           - auth-service
#           - user-service
#           - chat-service
#           - message-service
#           - realtime-service
#           - notification-service
#           - secrets-service
#           - audit-service
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.23"

#       - name: Run tests
#         working-directory: services/${{ matrix.service }}
#         run: go test ./... -v -race

#       - name: Build
#         working-directory: services/${{ matrix.service }}
#         run: go build -o /dev/null cmd/server/main.go

#   # ---------- Go Shared Packages ----------
#   go-packages:
#     name: Go Packages - Test
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         package:
#           - logger
#           - config
#           - auth-utils
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.23"

#       - name: Run tests
#         working-directory: packages/${{ matrix.package }}
#         run: go test ./... -v -race

#   # ---------- Python AI Orchestrator ----------
#   ai-orchestrator:
#     name: AI Orchestrator - Lint & Test
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.13"

#       - name: Install dependencies
#         working-directory: services/ai-orchestrator-service
#         run: pip install -r requirements.txt

#       - name: Lint with ruff
#         working-directory: services/ai-orchestrator-service
#         run: ruff check .

#       - name: Run tests
#         working-directory: services/ai-orchestrator-service
#         run: pytest tests/ -v

  # ---------- Frontend ----------
  # frontend:
  #   name: Frontend - Lint & Build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 10
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: pnpm
  #         cache-dependency-path: frontend/pnpm-lock.yaml
  #     - run: pnpm install
  #       working-directory: frontend
  #     - run: pnpm lint
  #       working-directory: frontend
  #     - run: pnpm build
  #       working-directory: frontend
